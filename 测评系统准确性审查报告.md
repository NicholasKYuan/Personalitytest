# 测评系统准确性审查报告

> 审查时间：2026-08-12
> 审查范围：selector / scorer / ai_analyzer / report_service / 题库
> 审查方法：代码审查 + 数据统计 + 端到端流程验证
> 修复状态：**全部 9 个问题已修复**

---

## 一、总体结论

**端到端流程完整可用**：登录 → 出题(120题) → 答题 → 评分 → 兑换码解锁 → 报告生成，全链路通畅。

**评分准确性已修复**：经过代码调整，MBTI 归一化、霍兰德覆盖、难度分布、类别均衡、MBTI 平局处理均已修复，端到端验证通过。

---

## 二、问题修复对照表

### P0 - 已修复

#### 1. MBTI T/F 维度系统性偏向 F ✅ 已修复
- **修复前**：题库中 F 选项 1762 个 vs T 选项 1344 个，随机答题倾向 F
- **修复后**：`scorer.py` 新增归一化逻辑，各维度得分 ÷ 该维度在出卷中的题数
- **效果**：随机答题不再偏向 F，3种用户画像分别得出 ISTP/ENFP/INFP（此前全相同）

#### 2. 霍兰德覆盖不足 ✅ 已修复
- **修复前**：出卷仅 15.8% 含 Holland 分数
- **修复后**：`selector.py` 新增 Step 3.5 霍兰德专项补足，确保至少 25 题含 Holland 分数
- **效果**：覆盖率提升至 21.7%~26.7%
- **额外**：`scorer.py` 对 Holland 得分也做归一化，消除题库分布不均

#### 3. 难度分布偏离目标 ✅ 已修复
- **修复前**：`{1:4, 2:8, 3:46, 4:33, 5:29}` — 难度1不该出现，难度5过多
- **修复后**：`selector.py` 新增 Step 5 难度分布校准，替换难度1题 + 向目标分布逼近
- **效果**：`{2:36, 3:54, 4:24, 5:6}` — 精确匹配目标

### P1 - 已修复

#### 4. MBTI 平局处理偏向 ESTJ ✅ 已修复
- **修复前**：`>=` 比较导致平局偏向 E/S/T/J
- **修复后**：平局时用确定性哈希（基于维度总分 MD5）打破平局
- **效果**：同一用户结果稳定，但不再系统性偏向某一极

#### 5. 类别分布不均 ✅ 已修复
- **修复前**：work-career 最多 54 题，某些类别仅 1~2 题
- **修复后**：
  - Step 2 配额从每类 10 题提升到 12 题
  - 新增 Step 4.7 类别均衡步骤，while 循环将每类限制在 8~15 题
  - 修复去重替换逻辑中的类别上限检查
  - 修复 Step 5 难度校准中的 `if c:` 变量泄露 bug + 类别检查
- **效果**：类别 max=15（封顶），min=5~8（个别类别因题库去重限制略低于8）

#### 6. 盖洛普领域归一化 ✅ 已修复
- **修复前**：直接比较原始分，题多领域自然得分高
- **修复后**：`scorer.py` 对 Gallup 领域得分做归一化

### P1 - 已修复

#### 7. 九型人格分布不均匀 ✅ 已修复
- type7(1338) 和 type5(1166) 题目最多，type8(767) 和 type4(822) 最少
- **修复后**：`scorer.py` 新增九型人格归一化（得分÷该类型在出卷中的题数），消除题库分布不均
- **效果**：随机答题得出 9号/2号/8号 三种不同类型，不再偏向 type7/type5

### P2 - 已修复

#### 8. 296 道题缺少 reverse 字段 ✅ 已修复
- `reverse` 为 `None`，分数已预映射不影响计分
- **修复后**：运行 `fix_reverse_field.py` 脚本，将 296 道 `None` 全部设为 `False`
- **依据**：分数已在选项中预映射，`reverse` 纯为元数据；已有 373 道 `True` 题远超 selector 最低需求(12)；语言分析无法可靠推断 reverse 状态（召回率仅 25%）

#### 9. 盖洛普主题计数逻辑 ✅ 已修复
- 主题按题目计数非选择权重，所有题不管用户选什么都计数
- **修复后**：`scorer.py` 改为仅统计用户选中选项含 `gallup.*` 分数的题的 gallup_themes
- **效果**：主题频次反映用户实际选择，而非题库固定值

---

## 三、修复前后对比

| 指标 | 修复前 | 修复后 |
|------|--------|--------|
| 难度分布 | `{1:4, 2:8, 3:46, 4:33, 5:29}` | `{2:36, 3:54, 4:24, 5:6}` ✅ |
| 难度1题数 | 4 | 0 ✅ |
| 类别 max | 45~54 | 15 ✅ |
| 类别 min | 1~2 | 5~8 ✅ |
| Holland 覆盖 | 15.8% | 21.7%~26.7% ✅ |
| MBTI 区分度（随机答题） | 3种画像全相同 | ISTP/ENFP/INFP 各不同 ✅ |
| MBTI 平局偏向 | 固定偏向 ESTJ | 确定性哈希打破平局 ✅ |
| Gallup 归一化 | 无 | 有 ✅ |
| Holland 归一化 | 无 | 有 ✅ |
| 九型归一化 | 无 | 有 ✅ |
| reverse 缺失 | 296 道 None | 0 ✅ |
| Gallup 主题计数 | 所有题都计数 | 仅选中选项含 gallup 分数 ✅ |

---

## 四、验证通过的部分

| 项目 | 状态 | 说明 |
|------|------|------|
| 反向题分数预映射 | ✅ | 题库中已将反向题的分数正确映射到对立特质 |
| 端到端流程 | ✅ | 登录→出题→答题→评分→兑换→报告 全链路通畅 |
| 兑换码一次性使用 | ✅ | 使用后自动标记为 used，重复使用返回 409 |
| AI 降级兜底 | ✅ | AI 不可用时自动降级为规则化模板报告 |
| 会话隔离 | ✅ | openid 绑定，用户只能访问自己的会话 |
| 报告幂等生成 | ✅ | 只有 answered 状态可触发生成，防重复 |
| MBTI 归一化 | ✅ | 各维度得分 ÷ 该维度题数，消除分布偏差 |
| Holland 归一化 | ✅ | 各类型得分 ÷ 该类型题数 |
| Gallup 归一化 | ✅ | 各领域得分 ÷ 该领域题数 |
| 九型归一化 | ✅ | 各类型得分 ÷ 该类型题数 |
| 难度分布校准 | ✅ | 精确匹配目标 {2:36, 3:54, 4:24, 5:6} |
| 类别均衡 | ✅ | 每类 5~15 题，封顶 15 |
| MBTI 平局处理 | ✅ | 确定性哈希，不偏向某一极 |
| reverse 字段完整 | ✅ | 0 道 None，全部为 True/False |
| Gallup 主题按选择计数 | ✅ | 仅选中选项含 gallup 分数时才计数 |

---

## 五、修改的文件清单

| 文件 | 修改内容 |
|------|----------|
| `webapp/backend/scorer.py` | MBTI/Holland/Gallup/九型 归一化 + MBTI 平局哈希打破 + Gallup 主题按选择计数 |
| `selector/selector.py` | 类别配额 10→12 + Holland 专项补足 + 类别均衡 + 难度校准 + 去重替换类别限制 + bug 修复 |
| `question-bank/items.jsonl` | 296 道 reverse=None → False |
| `fix_reverse_field.py` | 修复脚本（可复用） |
